"use client";

import { Button } from "@/components/ui/button";

/** Phase 1 stub; Phase 3: Web Push subscription. */
export function EnableNotificationsStub() {
  return (
    <Button type="button" variant="wireGhost" size="cta" className="mt-2 w-full" onClick={() => {}}>
      Enable notifications
    </Button>
  );
}
